# Tasks — Add Bot Introductions

## 1. Implementation

- [x] 1.1 `roleSuggestions.ts`: `starterOptions` per library role + `starterOptionsFor(roleDescription)` with keyword matching and generic fallback (+ tests)
- [x] 1.2 App create handlers seed the introduction (greeting + question card) for editor-create and quick-create (+ App tests: card renders, click sends)

## 2. Verification

- [x] 2.1 tsc + full suite green; walk delta scenarios
